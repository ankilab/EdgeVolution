value:
  - model: "nucleo_f439zi"  # Board model
    snr: "1050242564"  # Serial number
    ppk: "FAFD344C"  # PPK (Power Profiling Kit) serial number
    power_measurement_threshold: 24000  # Power consumption threshold (unit: mA) --> use tools/determine_power_consumption_threshold.py if needed
    max_available_tensor_arena_size: 155  # Available tensor arena size
